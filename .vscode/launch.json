{
    "version": "0.2.0",
    "configurations": [

        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "preLaunchTask": "build",
            "program": "${workspaceRoot}/target/debug/${workspaceRootFolderName}",
            "args": ["run", "--config-file", "${workspaceRoot}/lldap_config.toml"],
            //"cwd": "${workspaceRoot}/target/debug/",
            "sourceLanguages": ["rust"]
        }
    ]
}